import React from 'react';

const ToDoList = ({ data }) => {
    // <ul>
    //     {
    //         data.map((thing) => <li>{thing.text}</li>)
    //     }
    // </ul>
    
    console.log('data', data)
    return (<p>Something something dark side, something something complete<p/>)
};

export default ToDoList;